<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- ToolBar的Style Start -->
    <style name="ToolBarStyle" parent="">
        <item name="popupTheme">@style/ToolBarPopTheme</item>
        <item name="theme">@style/ToolBarTextSize</item>
    </style>

    <style name="ToolBarPopTheme" parent="@style/ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="ToolBarTextSize" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="AppTheme.DrawerArrowToggle" parent="Base.Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>
    <!-- ToolBar的Style End -->

    <style name="PopupWindowAnim">
        <item name="android:windowEnterAnimation">@anim/bottom_show</item>
        <item name="android:windowExitAnimation">@anim/bottom_dismiss</item>
    </style>

</resources>
